name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - uses: oven-sh/setup-bun@v2

      - name: Build UI
        run: cd ui && bun install && bun run build

      - name: Build all platforms
        run: |
          mkdir -p dist
          VERSION=${{ github.ref_name }}
          LDFLAGS="-s -w -X main.Version=${VERSION}"
          GOOS=linux GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o dist/etiquetta-linux-amd64 ./cmd/etiquetta
          GOOS=linux GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o dist/etiquetta-linux-arm64 ./cmd/etiquetta
          GOOS=darwin GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o dist/etiquetta-darwin-amd64 ./cmd/etiquetta
          GOOS=darwin GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o dist/etiquetta-darwin-arm64 ./cmd/etiquetta

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/etiquetta-linux-amd64
            dist/etiquetta-linux-arm64
            dist/etiquetta-darwin-amd64
            dist/etiquetta-darwin-arm64
          generate_release_notes: true
